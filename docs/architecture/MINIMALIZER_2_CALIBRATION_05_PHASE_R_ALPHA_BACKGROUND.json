{
  "policies": [
    {
      "condition": "opaque source + white background",
      "rationale": "This exactly matches the current V2 RGB renderer and keeps canonical opaque output unchanged.",
      "route_on_v2_default": "v2"
    },
    {
      "condition": "source contains material transparency",
      "rationale": "Retain source-alpha masking and subject-cutout behavior on the established legacy path.",
      "route_on_v2_default": "legacy"
    },
    {
      "condition": "background=source",
      "rationale": "Retain source-derived background color selection instead of pretending V2 white canvas has identical semantics.",
      "route_on_v2_default": "legacy"
    },
    {
      "condition": "background=transparent",
      "rationale": "Retain transparent output on the existing alpha-capable renderer.",
      "route_on_v2_default": "legacy"
    },
    {
      "condition": "background=custom",
      "rationale": "Retain explicit custom background colors on the legacy renderer until V2 exposes an equivalent contract.",
      "route_on_v2_default": "legacy"
    },
    {
      "condition": "ignore-source-alpha requested",
      "rationale": "Retain the legacy ignore-alpha semantics rather than silently changing how hidden RGB is interpreted.",
      "route_on_v2_default": "legacy"
    }
  ],
  "preserves_legacy_alpha_features": true,
  "preserves_opaque_white_pixels": true,
  "schema_version": "minimalizer-v2-alpha-background-v1",
  "v2_native_alpha": "file adapter composites RGBA onto white; renderer emits opaque RGB PNG",
  "v2_native_background": "white RGB canvas"
}
